name: 🚀 Deploy website to cPanel

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 🗂️ Ensure server directory structure
        run: |
          sudo apt-get install -y lftp -q
          lftp -c "
            set ftp:ssl-allow no;
            set net:timeout 10;
            open ftp://${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}@${{ secrets.FTP_SERVER_IP }};
            mkdir -fp project/bootstrap/cache;
            mkdir -fp project/storage/logs;
            mkdir -fp project/storage/debugbar;
            mkdir -fp project/storage/app/private;
            mkdir -fp project/storage/app/public;
            mkdir -fp project/storage/framework/cache/data;
            mkdir -fp project/storage/framework/sessions;
            mkdir -fp project/storage/framework/views;
            mkdir -fp project/storage/framework/testing;
          "

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          # Direct server IP — bypasses Cloudflare proxy which blocks FTP port 21.
          server: ${{ secrets.FTP_SERVER_IP }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          port: 21
          security: loose
          exclude: |
            **/.git*
            **/.git*/**

            # Claude Code config & memory
            .claude/**
            CLAUDE.md

            # Repo-level files not needed on server
            docker-compose.yml
            .docker/**
            .github/**
            README.md
            .commitsage

            # Laravel: secrets & local config
            project/.env
            project/.env.local
            project/.env.testing

            # Laravel: generated / installed locally
            project/vendor/**
            project/node_modules/**
            project/bootstrap/cache/*.php

            # Laravel: runtime storage (directories pre-created by lftp step above)
            project/storage/logs/**
            project/storage/debugbar/**
            project/storage/pail/**
            project/storage/framework/cache/**
            project/storage/framework/sessions/**
            project/storage/framework/views/**
            project/storage/framework/testing/**
            project/storage/app/public/**
            project/storage/app/private/**

            # Laravel: dev/test only
            project/tests/**
            project/phpunit.xml
            project/.editorconfig
            project/.vscode/**

            # Vite dev server leftover
            project/public/hot
